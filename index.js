 var patientCSV = "";
 var arrObj = [];
 var lines = patientCSV.split('\n');
 var headers = lines[0].split(',');

 for(var i = 0; i<lines.length; i++){
    varData = lines[i].split(',');
    arrObj[i] = {};
    for(var j =0; j<rowData.lenght; j++){
        arrObj[i][headers[j]]=rowData[j];
    }
    console.
 }
